# Project Notebook

## 2026-03-24

- 顶部导航折叠决策：`TopNavBar` 不做“第一行/第二行分别折叠”，而是统一做成“展开两行 / 收起一行摘要栏”的单折叠点。收起态必须继续保留项目入口、当前页面、当前会话摘要和设置/认证入口，避免为了省高度把导航能力彻底藏死；折叠状态持久化到 `localStorage['axon.topNavBar.collapsed']`，对应回归在 `src/web/client/src/components/swarm/TopNavBar/TopNavBar.test.tsx`。
- 顶部导航第二轮压缩决策：仅仅“可折叠”还不够，默认态也要优先节省垂直空间。新规则是默认进入单行紧凑态，并在紧凑态里保留整组 page tabs，确保用户不展开也能直接切页；完整两行只在需要项目/会话详细操作时展开。为了避免旧偏好把新紧凑默认态挡住，折叠状态键升级为 `localStorage['axon.topNavBar.collapsed.v2']`；回归继续落在 `src/web/client/src/components/swarm/TopNavBar/TopNavBar.test.tsx`。
- 顶部导航 A 方案落地规则：单行紧凑态必须明确分成三组，不再混排。左侧是项目上下文，中间是一级页面导航，右侧是“当前会话/新建/搜索”与账号状态；`新对话` 不能再伪装成导航 tab，搜索也不能再落到账号状态区。后续若继续压缩，只能在组内压缩，不能再次打散这三组边界。
- 顶部导航最终回退规则：如果用户要的是“点击按钮后上面两行全部不可见”，就不要再做紧凑态折中。正确实现是恢复原本两行结构，并提供一个单独的“隐藏顶部栏”操作；隐藏后顶部栏不占任何布局高度，只保留悬浮恢复按钮。状态单独持久化到 `localStorage['axon.topNavBar.hidden.v1']`，不要复用之前的 collapsed 键。
- 顶部栏隐藏按钮位置规则：隐藏前后的入口必须保持同侧，不能“右上角隐藏、左上角恢复”来回跳。当前恢复按钮固定在右上角，与原按钮保持同侧，减少视觉跳变和鼠标移动距离。
- Claude Code 本机登录导入规则：Web 端的 Claude 订阅登录不能再强制用户重复走手动回调；如果当前机器已经登录过官方 Claude Code，必须提供“一键导入本机登录”入口，直接把官方登录态写入 Axon 的 `oauthAccount` 并切到 `claude-subscription`。回归覆盖在 `tests/web/server/routes/auth-anthropic-import-local.test.ts` 和 `tests/web/oauth-login.test.tsx`。
- 官方 Claude Code 凭据位置坑：不能只假设登录态永远在 `~/.claude/.credentials.json`。官方源码当前会按 OAuth 环境后缀查找 `.credentials{,-custom-oauth,-staging-oauth,-local-oauth}.json`，macOS 上还可能通过 Keychain service `Claude Code*-credentials` 持久化。后续任何“导入 Claude Code 登录”改动都必须同时考虑文件和 Keychain 两条路径。
- 输入框收敛新规则：下一版 Web `InputArea` 继续参考 Codex 的“单一主输入面”结构，首层只保留附件、模型、思考强度、模式、语音、发送/停止；`新对话 / 锁定 / Probe / Logs / Git / Terminal / Transcript / Context / API usage` 统一进入 `更多` 抽屉或次级状态层，不能再常驻切碎主输入区。

## 2026-03-20

- `ConversationManager` startup stack overflow root cause: `getRuntimeProvider()` and `getRuntimeCustomModelName()` called each other while the constructor normalized the default model, so Web startup could fail before any session was created.
- Fix rule: runtime provider/model resolution must flow one way from `runtimeBackend + stored backend model + codex/custom fallback`, and must never infer provider by calling a custom-model getter that can call provider again.
- Regression coverage lives in `tests/web/server/runtime-selection.test.ts` and `tests/web/server/conversation.test.ts`.
- Frontend pitfall: assistant messages created locally in `useMessageHandler` must carry `runtimeBackend`, not just `model`; otherwise Axon Cloud sessions using GPT models render as `CODEX` because the UI falls back to model-based provider inference.
- Frontend pitfall: `useWebSocket` auto-restore stale session IDs from `localStorage` can legitimately fail after session deletion/cleanup; this must be handled inside the hook before broadcasting to app-level message handlers, otherwise startup shows a fake chat error even though the hook already self-healed by requesting a fresh session.
- 会话恢复坑：`handleSessionSwitch()` 会先把新 ws 挂到运行中的会话，再补发 pending 弹窗；如果 `AskUserQuestion` 恰好在这个窗口内已经实时发到新 ws，恢复逻辑就会重复补发一次。修复规则：pending user question 必须记录“是否已送达当前 ws”，恢复时只补发当前连接未收到的那批。
- Proxy privacy rule: OAuth proxy forwarding must strip client-origin IP headers such as `x-forwarded-for`, `x-real-ip`, `cf-connecting-ip`, `forwarded`, and `true-client-ip`; otherwise a reverse proxy in front of Axon can leak downstream client IPs to Anthropic.
- Regression coverage for this lives in `tests/proxy/ip-header-sanitization.test.ts`.
- Codex 本机登录坑：`~/.codex/auth.json` 不只会存 ChatGPT OAuth token，也可能只有 `OPENAI_API_KEY`。导入逻辑必须同时支持这两种结构，否则“导入本机登录”会误报 `No usable Codex credentials found in auth.json`。
- Codex 运行时规则：ChatGPT OAuth 继续走 `https://chatgpt.com/backend-api/codex`；如果是 `OPENAI_API_KEY` 这类 Codex API key 登录，默认应走 `https://api.openai.com/v1`，且允许显式 `/v1` 覆盖地址。相关回归测试在 `tests/web/server/codex-auth-manager.test.ts` 和 `tests/web/server/web-auth.test.ts`。
- Codex 本机配置导入规则：`~/.codex/auth.json` 只负责凭证，`~/.codex/config.toml` 负责模型和 provider/base_url；Web UI 的“导入本机登录”必须同时导入两者，并把 `model` 同步到 `defaultModelByBackend['codex-subscription']`，否则自定义 provider 会变成“凭证导进来了，但请求还在用旧地址/旧模型”。
- Codex Responses 流式坑：`response.output_item.added` / `response.function_call_arguments.done` 已经把工具调用流给前端后，`response.completed` 里的同一份 `function_call` 不能再完整重放，否则会在聊天转录里生成第二张一模一样的工具卡片；正确策略是 `response.completed` 只补发流里缺失的部分（如漏掉的 `tool_use_complete`）并负责 `usage/stop` 收尾。回归测试在 `tests/web/server/codex-client.test.ts`。

## 2026-03-21

- Skills 热重载坑：`src/tools/skill.ts` 之前的 `enableSkillHotReload()` 只是占位日志，`initializeSkills()` 又只按首次 `cwd` 加载一次全局缓存，结果当前会话新装/新改的 skill 不会自动生效，切到另一个项目目录时还会继续沿用上一个项目的 project skills。
- 修复规则：skills 缓存必须绑定 `cwd + add-dir` 上下文，至少在上下文变化时重载；文件热重载不能只清缓存，必须真实 watch `~/.axon/skills` / 项目 `.axon/skills` / builtin / plugin skill 源并把创建、修改、删除都回灌进注册表。回归覆盖在 `tests/skill-hot-reload.test.ts`。
- Web 会话切换性能坑：`useWebSocket` 之前会把所有带 `sessionId` 的流式消息先广播给所有前端 handlers，再由更上层的 `useMessageHandler` 晚过滤；当两个会话都在运行时，后台会话的 `text_delta/tool_use_*` 也会触发 App、TTS 等副作用，切换时主线程明显卡顿。
- 修复规则：会话隔离要前移到 WebSocket 分发入口，只把当前激活会话需要的高频流式消息分发给 handlers；跨会话仍需保留 `status / permission_request / user_question` 这类提醒信号。回归覆盖在 `tests/web/useWebSocket-recovery.test.tsx`。
- Web UI 思考配置规则：输入框里的“思考开关/强度”不能只停留在前端状态，必须跟随每条 `chat` 消息一路透传到 `websocket -> ConversationManager -> runtime client`，否则用户看到开关变化但实际请求仍会沿用默认思考参数。
- 排队消息坑：上下文压缩期间缓存的待发送消息也必须携带“排队当时”的 `thinkingConfig` 快照，不能等真正发送时再读最新 UI 状态，否则用户在等待期间切换强度会让发出去的请求配置漂移，形成难复现的错配。
- OpenAI 思考档位规则：Web UI 不能把所有模型都硬编码成同一组思考强度；`GPT-5.4` 这类支持 `xhigh` 的 Codex/GPT-5 模型要显示 `xhigh`，而不支持该档位的模型必须自动收敛到自己的最高可用档位，避免 UI 允许选择但 runtime 实际不接受。
- AutoCompact 前端悬挂坑：`performAutoCompact()` 在内部吞掉压缩失败并返回 `wasCompacted: false` 时，Web 端不一定能收到 `context_compact:end/error`；如果后续会话已经继续流式输出，前端必须把停留在 `compacting` 的旧状态自愈清掉，否则输入框会一直把后续消息当作“压缩排队”。
- Codex 自定义网关兼容规则：`GPT-5.4` 走自定义 `/v1/responses` 时，代理可能直接返回 Cloudflare 524 之类的 HTML 错页；runtime client 不能把整页 HTML 原样抛给日志/前端，应该提炼成短错误，并在自定义 Responses 端点场景优先回退到 `/v1/chat/completions`。回归覆盖在 `tests/web/server/codex-client.test.ts` 和 `tests/web/useMessageHandler-compact-recovery.test.tsx`。
- ImageGen 上传图引用坑：Web 输入框里的图片附件如果只作为视觉内容传给模型、却没有同时保存成稳定的临时文件映射，模型一旦在工具调用里填了 `image_path` 就很容易退化成“只有文件名的相对路径”，最终在工作区根目录 `stat` 失败。修复规则：上传图片要同时保留 `name -> temp file path` 映射，并在执行 `ImageGen` 前把无效的相对 `image_path` 回绑到最近上传图；回归覆盖在 `tests/web/server/image-attachments.test.ts`。
- ImageGen 参数校验规则：`generateImage()` 必须先校验 `image_path` 是否存在、以及 `image_path/image_base64` 是否互斥，再初始化 Gemini client；否则本地输入错误会被 `GEMINI_API_KEY` 缺失这种远端前置条件掩盖，用户看不到真正可操作的报错。回归覆盖在 `tests/web/server/gemini-image-service.test.ts`。
- npm 发包规则：生产态 Web Server 运行时会从包内读取 `src/web/client/dist`；因此 npm 包的 `files` 必须显式包含该目录，且 `prepublishOnly` 必须先构建前端再发布，否则 `axon-web` 安装后只会返回 “Frontend Not Built”。回归覆盖在 `tests/release/npm-package-manifest.test.ts`。
- 发版决策：`v2.4.0` 之后已经累计了运行时配置、Web 会话隔离、思考配置透传、ImageGen 附图映射等一组新能力，不适合继续走补丁号；后续若基于这批改动发版，默认从 `2.4.x` 升到 `2.5.0`，并同步 `package.json`、`package-lock.json`、`electron/package.json` 三处版本源。
- 发布流水线踩坑：`.gitignore` 里的敏感信息规则 `**/*token*` 会误伤正常源码文件；`src/core/max-tokens.ts` 本地存在但未被 git 跟踪，导致 Windows 本机 `tsc` 能过、GitHub Actions 在干净检出上却报 `Cannot find module './max-tokens.js'`。修复规则：对确需保留的源码文件加精确白名单（当前为 `!src/core/max-tokens.ts`），并用发布回归测试同时检查白名单存在、文件已被 git 跟踪；基于已推送失败的 `v2.5.0` 改正时，优先发补丁版 `v2.5.1`，不要改写已公开 tag 历史。
- 新手目录选择规则：Web 首次进入时，如果用户既没有 `localStorage` 里保存的项目，也没有最近项目记录，就必须保持“无项目欢迎态”，不要自动把服务端/安装目录当成当前项目打开；否则新用户会直接掉进陌生目录，看不到“打开文件夹”的主路径，也自然不知道如何切换目录。回归覆盖在 `tests/web/project-context-initial-selection.test.ts` 和 `tests/web/project-selector.test.tsx`。
- 首次使用引导规则：认证成功不等于 onboarding 完成，`axon_setup_done` 只能在用户真正完成 usage onboarding 并进入产品时写入；否则刷新页面会把“先选项目目录”这条关键引导直接跳过。
- 新手 onboarding 决策：`SetupWizard` 最后一屏必须是可操作的 usage onboarding，而不是静态 tips；至少要明确引导三件事：先打开项目目录、再在聊天里描述目标、最后根据任务切到 Code / Blueprint / Swarm。第一步必须提供真实 CTA，并与当前已选项目状态联动。回归覆盖在 `tests/web/setup-wizard.test.tsx`。
- 模型上下文窗口规则：GPT/Codex 兼容模型的默认上下文和输出上限不能再散落在 `loop/session/mcp/context/max-tokens` 多处硬编码里；至少这批官方模型要统一走中心规格表：`gpt-5.4`=1,000,000，上下文；`gpt-5.4-mini` / `gpt-5-codex` / `gpt-5.3-codex` / `gpt-5.2` / `gpt-5.2-codex` / `gpt-5.1` / `gpt-5.1-codex` / `gpt-5.1-codex-max`=400,000，上限输出统一 128,000。
- 可配置覆盖规则：新增 `modelContextWindowById` 后，按模型 ID 的上下文窗口覆盖必须在所有上下文预算入口统一生效，包括 auto-compact、session stats、MCP tool search 阈值、增强上下文窗口统计和 Web 设置页；不要只改某一个调用点，否则不同模块会继续算出不一致的预算。
- 浏览器导入陷阱：`src/config/axon-md-parser.ts` 这类会被 Web 端间接打进 bundle 的共享模块，不能在模块初始化或构造默认值里直接读取 `process.cwd()` / `process.env`；浏览器里 `process` 不存在，会在 `new AxonMdParser()` 甚至顶层单例导出阶段直接抛 `ReferenceError`。修复规则：Node 全局访问必须包进 runtime guard，并提供浏览器安全 fallback；回归覆盖在 `tests/config/axon-md-browser-safety.test.ts`。
- Web 共享层依赖边界：`src/web/shared/*.ts` 不能再 import `src/models/config.ts`、`src/config/index.ts` 这类服务端配置模块；哪怕只是为了拿一个能力判断，也会经由 `model-limits -> configManager -> axon-md-parser` 把 Node-only 依赖整条卷进浏览器，最终在前端触发 `process/path/fs` 运行时错误。修复规则：Web 共享模块只能依赖纯函数/纯数据模块；当前回归覆盖在 `tests/web/shared/thinking-config-browser-boundary.test.ts`。
- Web 大会话渲染性能坑：reload / session_switch 虽然已经把跨会话流式噪音挡在 `useWebSocket` 入口，但前端仍会在 `session_switched/history` 后同步把整段聊天历史一次性挂到 DOM，触发成批 `Message -> MarkdownContent -> tool card` 渲染和 markdown 高亮，历史长一点时切会话、F5 恢复都会明显卡主线程。
- 修复规则：大历史会话必须先渲染最近一屏，再用空闲时间分批补齐更早消息；同时消息组件要尽量保持稳定引用，避免 hydrate 期间反复重跑 markdown 解析。当前回归覆盖在 `tests/web/useProgressiveMessageRendering.test.tsx`，构建验证通过 `npm --prefix src/web/client run build`。
- Codex 思考流兼容规则：`responses` 流不能只监听 `response.reasoning.delta` / `response.reasoning_summary_text.delta` 两种事件；官方还会发 `response.reasoning_summary.done`、`response.reasoning_summary.delta`、`response.reasoning.done`、`response.reasoning_summary_part.done`、`response.output_item.done` 等快照事件。漏接这些时，Web 端会表现成“明明开了思考，但前端完全不显示”。回归覆盖在 `tests/web/server/codex-client.test.ts`。
- Web 思考历史持久化规则：流式 thinking 不能只发给前端回调，还必须写回 `assistantContent` 并在 `convertMessagesToChatHistory()` 里映射成 `{ type: 'thinking', text }`；否则当前轮次也许能临时看到思考，但刷新、切会话、历史重发后会整段消失。
- GPT-5.4 / 兼容网关工具流踩坑：同一个工具调用在 `responses` 流里可能前面用 `response.output_item.added.item.id`，后面又在 `response.function_call_arguments.done.call_id` / `response.completed.output[].call_id` 里换另一套标识；如果 runtime 只按原值透传，前端会把同一次 `Edit` 识别成两次工具调用，出现重复工具卡甚至后续状态对不上。修复规则：后端必须把 `id/call_id` 统一归一化成同一个 canonical tool id，并且 `response.completed` 只能补发流里缺失的工具收尾，不能把已经发过的 start/complete 再 replay 一遍。
- Chat Completions fallback 兼容规则：部分自定义 `/v1` 网关会在 SSE 里重复回放同一个 `tool_call.id`，甚至把 `function.arguments` 作为“累计快照”而不是纯 delta 重发；聚合逻辑如果只看 `index` 并直接字符串拼接，就会凭空造出第二个 `Edit` 或把 JSON 参数拼坏。修复规则：fallback 流优先按原始 `tool_call.id` 复用已有工具，对 snapshot 型 arguments 只提取新增部分。
- 前端兜底规则：`useMessageHandler` 收到重复的 `tool_use_start` 时，若同 `id` 的 tool card 已存在，必须原位更新而不是继续 append。后端去重是主修复，前端按 `id` 防重是最后一道保险。回归覆盖在 `tests/web/server/codex-client.test.ts` 和 `tests/web/useMessageHandler-tool-dedup.test.tsx`。
- 多供应商配置坑：Web 当前并不是“所有 provider 一等公民”，真实在线路上同时存在 `runtimeBackend`、`runtimeProvider`、`apiProvider` 三套概念，而且不少分支还会继续按 `runtimeProvider === 'codex'` 或模型名前缀来猜能力，导致 OpenAI-compatible 这类后端虽然能跑对话，却在“测试连接 / 拉模型列表”这些外围链路上被误判成不支持。
- 第一阶段收敛规则：先不要做大一统重构，优先把真实在线路上的能力判断收敛到 `runtimeBackend`。凡是“是否支持动态模型目录 / 该走哪种连接测试 / 是否需要默认 base URL”的判断，都应尽量由 backend capability 决定，而不是由 runtime family 或模型名正则推断。
- 动态模型目录规则：`axon-cloud` 和 `openai-compatible-api` 都应视为支持 runtime model catalog，前后端必须共用同一套 helper 生成 `/models` 端点并解析返回；尤其 base URL 允许用户直接填到 `/v1`，拼接模型目录时必须避免产出 `/v1/v1/models`。回归覆盖在 `tests/web/server/runtime-model-catalog.test.ts`、`tests/web/server/api-manager.test.ts` 和 `tests/web/runtime-model-catalog.test.ts`。
- API 测试连接规则：`/api/config/api/test` 不能再默认走 Anthropic SDK；它必须先根据 `runtimeBackend/apiProvider` 解析出真正的 runtime backend，再选择对应 runtime 做最小请求，并把 `provider + availableModels` 一起返回给前端，这样 OpenAI-compatible 才能在配置页里完成真实联通性校验。回归覆盖在 `tests/web/server/api-connection-test.test.ts`、`tests/web/shared/setup-runtime.test.ts` 以及配置页相关前端测试。
- Runtime provider 解析规则：`webAuth.getRuntimeProvider()` 不能只用 `runtimeBackend` 推断，尤其 `axon-cloud` 这类同一 backend 可承载 Claude / GPT / DeepSeek 混合模型；必须把“当前 backend 的已配置模型”一起传给 `getProviderForRuntimeBackend()`，否则 `Axon Cloud + GPT` 会被误报成 `anthropic`，并继续影响 `/api/models`、状态回退和失败兜底分支。
- `/api/models` 收敛规则：REST 模型列表不能再只拼静态推荐项；它必须复用 `apiManager.getAvailableModels()` 的动态目录结果，并把 `availableModels` 透传到统一的 runtime model list builder，再由共享 `model-catalog` 决定“动态目录优先、静态推荐兜底”。这样 OpenAI-compatible / Axon Cloud 的 REST 链路才会和 websocket `api_models_response` 保持一致。回归覆盖在 `tests/web/server/runtime-model-list.test.ts`。
- 配置页模型目录规则：`ApiConfigPanel` 里的模型下拉不能只依赖静态推荐或历史 settings；打开配置页时，如果当前 backend 支持动态目录，应主动读取 `/api/models` 做一次 hydration；如果 `/api/config/api/test` 返回 `availableModels`，也要立即写回 `customModelCatalogByBackend[runtimeBackend]`，这样测试成功后下拉选项会立刻刷新，并在后续保存时持久化。回归覆盖在 `tests/web/api-config-panel.test.tsx`。
- Runtime 归一化规则：共享 `normalizeRuntimeConfigShape()` 不能把 `current` 和 `updates` 直接无脑 merge 后就优先信任 `runtimeBackend`，也不能让 `updates` 里的 `undefined` 把当前 `apiProvider/runtimeProvider/authPriority` 覆盖掉；否则“oauth ↔ apiKey 切换”不会真正跨 family 收敛，而“只保存普通字段”又会把当前 backend/provider 意外打回默认值。修复后前后端都必须只把“显式更新的 runtime 字段”交给 helper，回归覆盖在 `tests/web/shared/setup-runtime.test.ts`、`tests/web/server/config-service.test.ts`、`tests/web/auth-dialog.test.tsx` 和 `tests/web/api-config-panel.test.tsx`。
- 子 agent 模型选择规则：`TaskTool` 不能只把 `resolveAgentModel()` 的结果直接塞进 `ConversationLoop`；这个函数只会处理 `inherit / 默认值 / 父模型`，不知道当前 runtime backend。当 Web 运行在 `codex-subscription` / `openai-compatible-api` 下时，像 `code-analyzer` 这种内置子 agent 的默认 `opus` 必须先按 runtime selection 归一化到当前 GPT/Codex 模型，否则子 agent 会继续拿 Claude alias 走错 provider，最终出现“0 tool calls, no text output”或直接不工作。回归覆盖在 `tests/tools/agent.test.ts`。
- 子 agent runtime client 规则：修完模型归一化还不够，`ConversationLoop` 之前在构造和重建 client 时仍会固定 `new ClaudeClient(...)`，所以 Web 主会话哪怕已经跑在 `codex/openai-compatible`，`TaskTool` / `TaskManager` 拉起的子 agent 还是会掉回 Anthropic 协议栈。修复规则：子 agent 必须直接透传完整 `ConversationClientConfig` 给 `ConversationLoop`，让 loop 按 provider 选择 `CodexConversationClient` 或 `ClaudeClient`；相关回归覆盖在 `tests/core/loop-runtime-client.test.ts`、`tests/tools/agent-stream-failure.test.ts` 和 `tests/web/server/task-manager.test.ts`。
- 子 agent 失败态规则：`ConversationLoop` 流式请求彻底失败时会产出一个“没有 `toolName`、只有 `toolError` 的 `tool_end`”，这不是普通工具失败，而是整条子 agent loop 已经死掉。`TaskTool` / `TaskManager` 必须把这种事件升级成任务失败，不能再收尾成 `Task completed (0 tool calls, no text output)`。本次回归同样覆盖在 `tests/tools/agent-stream-failure.test.ts` 和 `tests/web/server/task-manager.test.ts`。
- Codex 工具输入流规则：`responses` / fallback `chat completions` 并不保证总有 `tool_use_delta`，很多情况下只有最终的 `tool_use_complete`；`ConversationLoop` 必须接受 complete 事件里的整包参数并覆盖工具输入，否则 Codex 子 agent 即使走对 provider，也可能拿空 `{}` 去执行工具。
- Web 强制重启恢复坑：前端可能还保存着 `session_new` 生成的临时 sessionId，但用户首条消息其实已经在服务端创建成真正的持久化 session；如果这时进程被强杀，客户端来不及收到 `session_created`，重启后再拿临时 ID 恢复就会误报“Session does not exist”。修复规则：持久化会话创建时必须把旧临时 ID 一并记成 alias，恢复时优先把临时 ID 解析回真实 session；前端对 `resume failed` 这类同属恢复失败的文案也要走自愈分支。回归覆盖在 `tests/web/server/session-manager.test.ts` 和 `tests/web/useWebSocket-recovery.test.tsx`。
- 多模型架构决策：项目已经从“Claude 主干 + 兼容层”进入“多 runtime 并存”阶段，后续重构不应再按 provider 名字或模型前缀继续补丁式扩展，而应正式收敛到 `runtimeBackend -> provider protocol -> normalized model -> capabilities` 四层模型。`runtimeBackend` 负责线路选择，`provider` 只表示协议族，动态模型目录 / thinking 档位 / 默认 base URL / 工具流语义等都必须走 capability 表；子 agent、ScheduleTask、Blueprint worker 必须直接复用父级 runtime snapshot，不再自行猜测。整体收敛文档落在 `docs/runtime-architecture-convergence.md`。
- 第 1 阶段收敛落地：新增纯共享 `src/web/shared/runtime-capabilities.ts` 作为 backend capability 注册表，先把“provider routing / 动态模型目录 / 默认 base URL / 默认测试模型 / OAuth 刷新策略 / utility 模型策略”集中到一个地方，再由 `model-catalog`、`web-auth`、`api-connection-test`、`api-manager`、`utility-client` 复用。规则是：先抽中心规则，再迁移核心消费者，不在第一步就大拆 `ConversationManager`。
- 前端 runtime 配置收口规则：`AuthDialog`、`SetupWizard`、`SettingsPanel` 这些入口不能各自维护一套 backend 分组和认证分支；backend 列表、auth mode、provider choices、默认 payload 都要复用 `src/web/shared/setup-runtime.ts`。尤其设置页的“默认模型 backend 列表”不能漏掉 `axon-cloud`，动态 backend（`axon-cloud` / `openai-compatible-api`）还要把已保存的 `customModelCatalogByBackend` 一起带进模型下拉，否则 UI 会悄悄退回静态推荐集。回归覆盖在 `tests/web/auth-dialog.test.tsx`、`tests/web/setup-wizard.test.tsx` 和 `src/web/client/src/components/__tests__/SettingsPanel.test.tsx`。
- Legacy runtime 归一化坑：`normalizeRuntimeConfigShape()` 是“把待更新/待迁移的 runtime 字段归一化”的 helper，不是“拿 current 状态读默认值”的 helper。服务端用它迁移旧 `settings.json` 时，必须把 legacy `runtimeBackend/runtimeProvider/apiProvider/authPriority` 当成 `updates` 传入；如果误传到 `current`，helper 会因为看不到 runtime update signal 而回落到默认 `claude-compatible-api`，直接把旧的 `codex + apiKey` 配置误判成 Claude family。回归覆盖在 `tests/web/server/web-auth.test.ts`。
- Axon Cloud 充值链路决策：Electron 里不要再把充值留在内嵌窗口或直接跳 `https://api.chatbi.site/console/topup`，因为 NewAPI 控制台登录态依赖该域名自己的 `localStorage.user`，本地 Axon 无法安全注入，用户容易被迫二次登录。正确做法是：前端充值按钮统一打开本地 `/api/axon-cloud/topup` 桥接页，服务端用登录时缓存的 `accessToken + userId` 直接调用 NewAPI 的 `/api/user/topup/info` 和 `/api/user/creem/pay` 生成 Creem checkout，再由 Electron 用系统浏览器接管支付。相关回归覆盖在 `tests/web/server/axon-cloud.test.ts` 和 `tests/web/auth-status.test.tsx`。
- Runtime model capability 规则：`supportsDynamicModelCatalog` 还不够表达 slash / session 的真实行为，动态 backend 还需要单独声明“允许任意模型 ID”。当前 capability 表里 `axon-cloud` 和 `openai-compatible-api` 必须显式标记 `allowsArbitraryModelIds=true`，这样 `/model`、runtime selection、会话恢复等链路才不会继续拿 `provider === 'codex'` 去误拒 `deepseek-v3` / `qwen-max` 这类真实模型。回归覆盖在 `tests/web/shared/runtime-capabilities.test.ts` 和 `tests/web/server/slash-commands.test.ts`。
- Runtime selection 路由规则：`resolveRuntimeSelection()` 选择 provider 时必须以“本次实际归一化后的 model”作为输入，不能优先看 `customModelName` / 存储的默认模型；否则 `Axon Cloud` 这类 model-routed backend 只要默认模型是 GPT、当前会话临时切到 `opus`，就会被错误路由回 codex 协议栈。另一个配套规则是 `openai-compatible-api` 不能再忽略 legacy `customModelName`，并且动态 backend 在会话创建/恢复时要允许带入 `customModelCatalogByBackend`。回归覆盖在 `tests/web/server/runtime-selection.test.ts` 和 `tests/web/server/conversation.test.ts`。
- Slash runtime 状态规则：`/status`、`/config` 不能再只看环境变量里的 Anthropic key；在多 runtime 架构下，它们必须直接读取 `webAuth.getStatus()` 和当前 session runtime backend，否则 Codex OAuth、OpenAI-compatible API、Axon Cloud 都会被错误显示成“未连接”或“未知 backend”。回归覆盖在 `tests/web/server/slash-commands.test.ts`。
- Runtime client 优先级规则：`ApiManager`、`utility-client` 这类外围 runtime client 不能自己再拼一套“preferred model / custom model / backend default / catalog”优先级，必须直接复用 `resolveRuntimeSelection()`。动态 backend 的优先级现在应为：先用 `defaultModelByBackend` / legacy `customModelName`，再用已保存的 `customModelCatalogByBackend`，最后才回退到 backend 自己的默认 test model。这样 `ApiManager`、`utility-client`、会话恢复、slash `/model` 才不会对同一个 backend 算出不同模型。回归覆盖在 `tests/web/server/api-manager.test.ts`、`tests/web/server/utility-client.test.ts` 和 `tests/web/server/utility-client-backend-defaults.test.ts`。
- Runtime 状态展示规则：`/api/models`、`ApiManager.getStatus()`、`ApiManager.getProviderInfo()` 不能再从 `webAuth.getRuntimeProvider()` 或 Anthropic/Codex 默认地址各自猜当前线路；它们必须统一从 `runtimeBackend + resolveRuntimeSelection() + credentials` 生成同一份 runtime snapshot。否则 `openai-compatible-api` / `axon-cloud` 在异常回退或动态 catalog 缺失时会被错误显示成 `https://api.anthropic.com`。回归覆盖在 `tests/web/server/api-manager.test.ts` 和 `tests/web/server/runtime-model-list.test.ts`。
- 动态 backend 展示规则：设置页里的模型说明卡片不能只看 `runtimeProvider === 'anthropic'`；像 `axon-cloud` 这类动态 backend 即使当前选中的是 Claude 模型，也必须继续按 runtime model catalog 展示真实可用模型，而不是退回固定的 Opus/Sonnet/Haiku 三张卡。回归覆盖在 `src/web/client/src/components/__tests__/SettingsPanel.test.tsx`。
- 认证摘要语义规则：`provider === 'codex'` 不等于“ChatGPT / Codex 订阅账号”；只有 Codex OAuth/订阅态才显示 ChatGPT/Codex 身份，Codex/OpenAI 兼容 API key 仍应显示为 `API Key`。回归覆盖在 `tests/web/shared/auth-summary.test.ts`。
- AskUserQuestion 前端兜底规则：Web 端不能假设 `user_question` payload 永远是完整的 `{ requestId, question, header, options }` 单一形态。真实链路里可能出现字段别名（`prompt/text/title/id`）或嵌套 `questions[0]` 恢复态；如果前端直接渲染 `question.question`，弹窗就会只剩默认标题和空白输入框。修复规则：进入 UI 前先做 payload 归一化，最差也要显示明确的 fallback 文案，绝不能出现“用户不知道在回答什么”的空白弹窗。回归覆盖在 `tests/web/user-question-normalizer.test.ts` 和 `tests/web/user-question-dialog.test.tsx`。

## 2026-03-22

- Edit 工具契约坑：`src/tools/file.ts` 曾经同时支持 `batch_edits` 和单次 `old_string/new_string`，但 schema 仍把顶层 `old_string/new_string` 设成必填，执行层又禁止它们与 `batch_edits` 同时出现，导致 GPT-5.4 被 schema 诱导填入 `'' / unused / placeholder` 占位后反复触发 `Cannot combine batch_edits with top-level old_string/new_string edits.` 并连续重试工具。
- OpenAI/Codex function schema 限制：顶层参数 schema 必须是平铺 `type: object`，不能在顶层使用 `anyOf/oneOf/allOf`。因此 Edit 不能靠顶层组合 schema 表达“双模式”，最终规则是保留平铺 object、只要求 `file_path`，再由执行层自己校验“必须提供单 edit 或 batch edit 其中一种”。
- 修复规则：Edit 执行层要在入口统一判定 batch 模式，只拒绝“真实顶层单 edit 与 batch 混用”，对 schema 诱导出来的空串或 `unused/placeholder` 占位直接忽略，避免在普通文本、Office 文档和文件不存在分支上出现语义分叉。
- 回归覆盖在 `tests/tools/file.test.ts` 和 `tests/web/server/tool-input-normalizer.test.ts`；另外 legacy `ToolDefinition.inputSchema` 类型必须复用 `ToolInputSchema`，否则 TypeScript 会错误拒绝合法的 `anyOf` JSON Schema。
- Web UI 渲染坑：`src/web/client/src/components/CliToolCall.tsx` 之前的 `EditToolContent` 只读取顶层 `old_string/new_string`，完全忽略 `batch_edits`。OpenAI/Codex 兼容模型一旦走 batch edit，工具卡片头部会显示成功，但正文 diff 为空，只剩“已修改”，容易被误判成后端发了空编辑。修复规则：前端 `Edit` 卡片必须优先识别并渲染 `batch_edits`；如果拿不到任何可渲染 diff，还要退回显示 `tool_result.output` 文本，不能留下空白正文。回归覆盖在 `tests/web/cli-tool-call-hidden-output.test.tsx`。
- 第二个前端入口坑：`src/web/client/src/components/ArtifactsPanel/ArtifactsPanel.tsx` 的详情弹层和 `useArtifacts()` 之前仍停留在“只认顶层 `old_string/new_string`”的旧语义，所以 `Edit batch_edits` 在聊天卡片里已经能显示后，产物详情仍会渲染成 `0 changes` 空面板。修复规则：前端必须把 `Edit`/`MultiEdit` 的编辑提取逻辑收敛成共享 helper，并让 `useArtifacts` 同时保存 `editOperations + resultText`；详情弹层优先渲染多段 diff，没有可渲染 diff 时回退显示工具结果文本。回归覆盖在 `tests/web/use-artifacts.test.tsx` 和 `tests/web/artifacts-panel-edit-detail.test.tsx`。

## 2026-03-22

- 落地页下载提速决策：`/download/:filename` 不能再把“中国大陆提速”寄托在 Railway/GitHub 302 链路上；真正有效的方案是“国内镜像直链优先，GitHub Release 代理只做兜底”。实现上统一收敛到 `landing-page/download-utils.cjs`，由 Railway `server.js` 和 Vercel `api/download.js` 共用同一套区域识别、镜像优先级和 fallback 规则。
- 下载镜像配置规则：优先级固定为 `DOWNLOAD_MIRROR_CN_<FILE>_URL` → `DOWNLOAD_MIRROR_CN_BASE_URL` → `DOWNLOAD_MIRROR_<FILE>_URL` → `DOWNLOAD_MIRROR_BASE_URL` → GitHub 代理；如果某个镜像 URL 配错，不要静默跳过，直接报配置错误，避免“看似能下载，实际一直没走国内镜像”的假修复。
- 发版同步规则：想让中国用户真正变快，镜像端必须始终保留稳定文件名 `Axon-Setup.exe` / `Axon-Setup.dmg` / `Axon-Setup.AppImage`；只上传带版本号的产物而不维护稳定别名，落地页即使命中国内镜像也会直接 404。部署说明写在 `docs/landing-page-download-mirror.md`，回归覆盖在 `tests/landing-page/download-utils.test.ts`。
- 七牛尝试结论：曾短暂评估过七牛 Kodo 自动上传链路，但由于私有空间直链受限、自定义域名 HTTPS 额外折腾，最终没有作为长期生产方案保留。
- 生产下载链路陷阱：`office site / website` 这个 Railway 服务实际跑的是根目录 `Dockerfile` 构建出来的 Web Server，真正生效的下载路由是 `src/web/server/routes/download-proxy.ts`，不是 `landing-page/server.js`。如果只改落地页目录却不改这条 Express 路由，线上 `/download/:filename` 仍会继续直跳 GitHub。
- 生产镜像接入规则：真实生产路由也必须复用与落地页一致的区域识别和镜像优先级，并在 `GITHUB_TOKEN` 缺失时返回 mirror-only 资产列表；回归覆盖在 `tests/web/server/download-proxy.test.ts`。
- 七牛私有空间实测结论：`axon2` 当前是私有空间，测试域名 `tcadyhd1n.hd-bkt.clouddn.com` 会被七牛拦截 `exe/dmg/html/apk/ipa` 等应用下载，不能拿来做生产直链。
- 七牛技术限制结论：即便服务端实时签名 URL 可用，只要自定义域名 HTTPS 没落好，生产下载仍会遇到混合内容或浏览器拦截问题，因此最终还是切回腾讯云 COS 默认 HTTPS 域名。
- 免费镜像决策：如果目标是“尽量不折腾证书、国内直接 HTTPS 下载、发版自动覆盖稳定文件名”，优先补腾讯云 COS 默认 HTTPS 域名方案，而不是继续在七牛私有空间上绕签名 + 自定义域名 HTTPS。
- 腾讯云 COS 接入规则：GitHub Actions 额外支持 `TENCENT_COS_SECRET_ID/TENCENT_COS_SECRET_KEY/TENCENT_COS_BUCKET/TENCENT_COS_REGION` 自动上传 `Axon-Setup.exe` 到固定对象键；若未显式配置 `TENCENT_COS_PUBLIC_BASE_URL`，默认直接用 `https://<BucketName-APPID>.cos.<Region>.myqcloud.com/` 作为稳定下载地址。回归覆盖在 `tests/release/tencent-cos-utils.test.ts`。
- COS 权限规则：为了减少手工操作，上传脚本默认把 `Axon-Setup.exe` 对象 ACL 设为 `public-read`；这样即便存储桶维持“私有读写”，稳定安装包对象也能直接作为 Railway 的国内镜像直链。
- 下载镜像收敛决策：既然生产已经切到腾讯云 COS 并验证通过，仓库里不再保留七牛 Kodo 的双方案代码、依赖、workflow 和文档，避免后续发布时再次出现“代码一套、线上另一套”的维护分叉。
- GitHub Actions 解析陷阱：`secrets.*` 不能直接写进 workflow 的 `if:` 条件；哪怕 YAML 语法本身合法，GitHub 也会在解析阶段把整条 workflow 直接判成 invalid，表现为 run `0s` 失败且没有任何 job。修复规则：先把 secrets 映射到 job/step `env`，再在 `if:` 里判断 `env.*`；如果已公开 tag 因此发版失败，优先发补丁版，不要重写旧 tag。

## 2026-03-23

- Railway 官网部署踩坑：`landing-page/Dockerfile` 的健康检查不能依赖 `curl` 这类基础镜像未必自带的二进制；更稳的规则是直接用 Node 自带 `http` 发本地 `/api/health` 探针，这样镜像升级或 slim 基础镜像裁剪后不会因为“健康检查命令不存在”而把部署判死。
- Railway 构建优化规则：官网/演示站的 `Dockerfile.railway` 不需要下载 Electron 二进制，构建阶段应显式设置 `ELECTRON_SKIP_BINARY_DOWNLOAD=1`，并优先使用 `npm ci` / `npm --prefix ... ci` 走 lockfile 安装，减少 Railway 自动部署的下载体积、漂移和超时风险。回归覆盖在 `tests/release/railway-dockerfiles.test.ts`。
- Railway 版本判因规则：如果部署日志里明确打印 `axon-code@2.5.0 build`，同时报 `Cannot find module './max-tokens.js'`，优先判断“Railway 正在构建 2026-03-21 `673db46 fix: release 2.5.1 pipeline` 之前的旧 ref / 旧 tag”，不要误判成当前 `private_web_ui` 分支源码仍然缺 `src/core/max-tokens.ts`。当前仓库里该文件已存在且被 git 跟踪，问题在部署目标版本而不在工作树。
- Web 会话恢复/回退踩坑：F5 刷新或强制重启后，真正危险的不是单点报错，而是“两条链路叠加”。
  1. 客户端可能还握着临时 `sessionId`，而服务端真实会话已经持久化成另一个 ID；这时 `rewind` 或下一条 `chat` 如果直接信任客户端 ID，就会出现 `Session not found`，甚至把用户发的“继续”误创建成一个全新会话。
  2. `ProjectContext` 初始化恢复上次项目时，如果也被 `useSessionManager` 当成“用户主动切项目”，就会在会话恢复过程中额外发一个 `session_new`，把当前会话重新顶成临时会话，进一步放大上面的 alias 问题。
- 修复规则：WebSocket 侧所有需要操作现有会话的路径（至少 `chat`、`rewind_preview`、`rewind_execute`、`session_resume/session_switch`）都要先把临时 `sessionId` 解析回真实持久化会话；前端项目切换事件必须携带 `createSession/source` 元信息，初始化恢复项目默认 `createSession=false`，只有用户主动切项目时才自动新建会话。回归覆盖在 `tests/web/server/session-alias.test.ts`、`tests/web/server/session-manager.test.ts`、`tests/web/useSessionManager-project-change.test.tsx` 和 `tests/web/useWebSocket-recovery.test.tsx`。
- Web 当前会话 runtime 规则：认证状态接口返回的是“新会话默认 backend”，不是“当前已打开会话 backend”。如果前端把这两个概念复用成同一份 `runtimeBackend` 状态，切换登录方式后旧会话的输入区/模型选择会先被误切到新 backend，再和 websocket 里的旧会话 runtime 分叉，表现成“新会话正常、当前会话异常/误导”。修复规则：前端必须分离 `defaultRuntimeBackend` 和 `activeSessionRuntimeBackend`，当前 UI 永远优先显示 websocket 会话 runtime，只在会话尚未 ready 时才回退到默认 backend。回归覆盖在 `tests/web/useActiveRuntimeState.test.tsx`。
- Claude 订阅共享代理风控结论：当前 `src/proxy/server.ts` 的 OAuth 代理会把多个下游客户端折叠到同一个上游代理身份上。它会在进程启动时生成一次 `PROXY_DEVICE_ID` / `PROXY_SESSION_ID`，随后持续重写所有 `/v1/messages` 请求的 `metadata.user_id` 为 `user_<proxy-device>_account_<accountUuid>_session_<proxy-session>`，同时剥离 `x-forwarded-for/x-real-ip/cf-connecting-ip/forwarded/true-client-ip` 等来源 IP 头，只让上游看到代理出口 IP；但又保留下游原始 `User-Agent` / `x-stainless-*` 等客户端特征。结果是“多个真实用户/机器/并发请求”在上游视角被压成“同一账号 + 同一代理设备/会话 + 同一机房出口”，画像高度异常。官方 `@anthropic-ai/claude-code` 的确也会持久化 `userID` 并构造 `user_<device>_account_<account>_session_<session>`，所以风险不在“固定 device id”本身，而在“共享订阅 + 代理身份折叠 + 数据中心出口/区域不稳定 + 多客户端混合特征”这组信号叠加，更容易触发封禁/限制。
- 代理身份修复规则：共享 OAuth `/v1/messages` 代理不能再无条件重建整个 `metadata.user_id`，也不能在 metadata 缺失时替下游“代造身份”。当前最终规则是：只有下游已经提供合法 `metadata.user_id` 时，代理才允许转发，并且仅补全其中的 `accountUuid`，保留下游原有的 device/session；若 `metadata.user_id` 缺失或格式非法，直接拒绝请求。这样透传的 `User-Agent/x-stainless-*` 与 metadata 始终来自同一个真实客户端，避免共享代理把多客户端重新折叠成单一上游身份。回归覆盖在 `tests/proxy/ip-header-sanitization.test.ts`。

## 2026-03-24

- Web 会话恢复坑：`handleSessionSwitch()` 不能再用“最后一条消息是 `tool_result`”来推断是否需要自动续跑。用户取消、Abort 后的补偿 `tool_result` 也会满足这个条件，结果就是前端点了取消看似停了，F5 刷新又被误判成“应继续执行”。修复规则：续跑必须改成显式持久化标记，只在 SelfEvolve 这类受控场景置位；刷新恢复流式态时还要排除 `cancelled=true` 的收尾中会话。回归覆盖在 `tests/web/server/conversation.test.ts` 和 `tests/web/server/session-manager.test.ts`。
- Chat 输入区交互决策：底部输入框继续保留“默认折叠控制区”，但主操作区只放最核心的附件/发送/停止；锁定、探针、日志、Git、终端、历史等次级控制统一收进一条可展开的单行命令栏，避免主输入区被一排零散图标切碎。
- 输入区视觉规则：样式方向统一走“简约、克制、单块玻璃卡片”，不要再把输入框做成多层厚重按钮堆叠；命令栏展开态允许横向滚动，也不要在窄屏上直接换成多行碎布局。回归覆盖在 `src/web/client/src/components/__tests__/InputArea.test.tsx`。
