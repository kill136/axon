/**
 * 迁移系统类型定义
 *
 * 每个迁移函数必须是幂等的 — 多次执行不会产生副作用。
 * 这样即使迁移版本号没有正确更新，重复执行也是安全的。
 */

/**
 * 迁移函数接口
 * 每个迁移导出一个同步函数，返回 void。
 * 迁移本身负责判断是否需要执行（幂等性）。
 */
export interface Migration {
  /** 迁移的唯一标识名称 */
  name: string;
  /** 迁移函数 */
  run: () => void;
}
